# Vercel needs server and shared files for API functions
# Don't ignore them!

# Only ignore these:
node_modules
.env*
*.log
dist/public
.vscode
.idea
*.mjs

# api/index.ts is used by the build script and then deleted to avoid conflicts
# Only api/index.js (the built file) will remain for Vercel
