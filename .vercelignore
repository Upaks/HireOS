# Vercel needs server and shared files for API functions
# Don't ignore them!

# Only ignore these:
node_modules
.env*
*.log
dist/public
.vscode
.idea
*.mjs

# Note: api/index.ts source is now in scripts/api-index.ts to avoid Vercel conflicts
# The build script creates api/index.js from scripts/api-index.ts
# Vercel only sees api/index.js (no conflict!)
